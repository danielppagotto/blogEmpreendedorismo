knitr::opts_chunk$set(message=FALSE,warning=FALSE, cache=TRUE)
1+2
2*5
5/3
(1+6+4)/5
pesos <- c(65, 95, 75, 77, 80, 68)
alturas <- c(1.60, 1.78, 1.80, 1.68, 1.72, 1.65)
imc <- pesos/alturas**2
imc
round(imc, 2)
imc <- round(imc, 2) #estou sobrescrevendo um vetor
#arredondado sobre ele mesmo
imc
Matriz<-cbind(pesos,alturas,imc)
Matriz
df_imc <- as.data.frame(Matriz)
df_imc <- as.data.frame(Matriz)
View(df_imc)
cbind(nomes,df_imc)
nomes<-c("Alice","Gilmar","Cecilia","Bianca","Valentina","Augusto")
cbind(nomes,df_imc)
df_imc <- cbind(nomes,df_imc)
df_imc <- df_imc %>%
mutate(status = case_when(imc < 16 ~ "Subpeso Severo",
imc >= 16 & imc < 20 ~ "Subpeso",
imc >= 20 & imc < 25 ~ "Normal",
imc >= 25 & imc < 30 ~ "Sobrepeso",
imc >= 30 & imc < 40 ~ "Obeso",
imc >= 40 ~ "Obeso Mórbido"
))
library(dplyr)
df_imc <- df_imc %>%
mutate(status = case_when(imc < 16 ~ "Subpeso Severo",
imc >= 16 & imc < 20 ~ "Subpeso",
imc >= 20 & imc < 25 ~ "Normal",
imc >= 25 & imc < 30 ~ "Sobrepeso",
imc >= 30 & imc < 40 ~ "Obeso",
imc >= 40 ~ "Obeso Mórbido"
))
View(df_imc)
pesos <- c(56, 95, 75, 77, 80, 68)
pesos <- c(56, 95, 75, 77, 80, 68)
alturas <- c(1.60, 1.78, 1.80, 1.68, 1.72, 1.65)
imc <- pesos/alturas**2
imc
library(dplyr)
round(imc, 2)
imc <- round(imc, 2) #estou sobrescrevendo um vetor
#arredondado sobre ele mesmo
imc
Matriz<-cbind(pesos,alturas,imc)
Matriz
nomes<-c("Alice","Gilmar","Cecilia","Bianca","Valentina","Augusto")
df_imc <- as.data.frame(Matriz)
df_imc <- cbind(nomes,df_imc)
df_imc <- df_imc %>%
mutate(status = case_when(imc < 16 ~ "Subpeso Severo",
imc >= 16 & imc < 20 ~ "Subpeso",
imc >= 20 & imc < 25 ~ "Normal",
imc >= 25 & imc < 30 ~ "Sobrepeso",
imc >= 30 & imc < 40 ~ "Obeso",
imc >= 40 ~ "Obeso Mórbido"
))
pesos <- c(56, 95, 75, 65, 80, 68)
alturas <- c(1.60, 1.78, 1.80, 1.68, 1.72, 1.65)
imc <- pesos/alturas**2
imc
library(dplyr)
round(imc, 2)
imc <- round(imc, 2) #estou sobrescrevendo um vetor
#arredondado sobre ele mesmo
imc
Matriz<-cbind(pesos,alturas,imc)
Matriz
nomes<-c("Alice","Gilmar","Cecilia","Bianca","Valentina","Augusto")
df_imc <- as.data.frame(Matriz)
df_imc <- cbind(nomes,df_imc)
df_imc <- df_imc %>%
mutate(status = case_when(imc < 16 ~ "Subpeso Severo",
imc >= 16 & imc < 20 ~ "Subpeso",
imc >= 20 & imc < 25 ~ "Normal",
imc >= 25 & imc < 30 ~ "Sobrepeso",
imc >= 30 & imc < 40 ~ "Obeso",
imc >= 40 ~ "Obeso Mórbido"
))
install.packages("blogdown")
install.packages("blogdown")
